<div class="content-page">
    <div class="ui-title">{% helper lang.l('blacklist', 'blacklist') %}</div>
    <div>
        {% helper lang.l('field_usr_inf_description', 'blacklist') %}
    </div>
    {% for link, name in pages %}
        <div><a href="{{ link }}">{{ name }}</a></div>
    {% else %}
        {% if is_not_archive %}
            <input type="checkbox" name="ui_blacklist" data-module="blacklist" value="1" class="ui_checkbox" data-action="ui_change" checked>
        {% else %}
            <div><i>{% helper lang.l('no_blacklist', 'blacklist') %}</i></div>
        {% endif %}
    {% endfor %}
</div>
<div class="content-page">
    <div class="ui-title">{% helper lang.l('header_main', 'send_money') %}</div>
    <div class="ui-descr">
        {% helper lang.l('field_usr_inf_description', 'send_money') %}
    </div>
</div>
{% for item in your %}
    <div class="col-xs-12 content-page">
        <div class="ui-title bb">{% helper lang.l('send_money_gift_from', 'send_money') %}: {{ item.user.output_name }}</div>
        <div>{% helper lang.l('send_money_gift_for', 'send_money') %}: {{user.output_name }}</div>
        <div>{% helper lang.l('send_money_amount', 'send_money') %}: {{ item.amount }}</div>
    </div>
{% endfor %}